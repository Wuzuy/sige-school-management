spring.application.name=sige

# ============================================
# PROFILE SELECTION (Escolha: mysql ou h2)
# ============================================
# Para usar MySQL: spring.profiles.active=mysql
# Para usar H2: spring.profiles.active=h2
spring.profiles.active=h2

server.port=8080

# Configura��es de Seguran�a JWT
jwt.secret=SejaSenai2025SecretKeyForJWTTokenGenerationAndValidationMustBeLongEnoughForHS256
jwt.expiration=86400000

# Configura��es de Sess�o
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=strict

# ============================================
# CONFIGURAÇÕES DE EMAIL (Gmail SMTP)
# ============================================
# IMPORTANTE: Configure estas propriedades para habilitar recuperação de senha
# 1. Use uma conta Gmail dedicada para o sistema
# 2. Ative "Verificação em 2 etapas" na conta Google
# 3. Gere uma "Senha de app" em: https://myaccount.google.com/apppasswords
# 4. Use a senha de app gerada no campo spring.mail.password

# Email remetente (Gmail)
spring.mail.username=lucaslimasandin@gmail.com

# Senha de app gerada (não é a senha da conta!)
spring.mail.password=vtvu pvuh esym buhz

# Configurações SMTP do Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ============================================
# CONFIGURAÇÕES DO FRONTEND
# ============================================
# URL base do frontend para links em emails
app.frontend.url=http://localhost:5500
